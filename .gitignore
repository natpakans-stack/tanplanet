cody-bot-portfolio/
line-messaging-portfolio/
mandala-capture-portfolio/
parking-reminder-portfolio/
taiwan-guidebook-portfolio/
carpark_live_dashboard/
taiwan-v1-0/
talk-to-figma-mcp/
chatbot-farm/
.DS_Store
.vercel

# nested standalone project (own git repo / Vercel deploy) — not part of Pages build
Trip-CNX/
book-screenshot/
My-JiraTasks/

# ซอร์สโปรเจกต์ที่มีความลับ — ห้ามขึ้น repo สาธารณะนี้
# (AlphaX มี .env.local + ข้อมูลดีล · ring-bot มี .env และ deployment id
#  budget-bajjo มีบิลจริง/ชีตครอบครัว · shotdeck เป็นบรีฟร่าง)
# หมายเหตุ: ring-bot/ ถูก track อยู่แล้ว ใส่ตรงนี้ไม่มีผล — .env กับ .clasp.json ของมันยัง untracked อยู่
AlphaX/
budget-bajjo/
shotdeck/

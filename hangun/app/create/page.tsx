import { CreateProjectForm } from '@/components/CreateProjectForm';

export const metadata = { title: 'สร้าง Project — หารกัน' };

export default function CreatePage() {
  return <CreateProjectForm />;
}
